import random

password_length = int(input("Enter the length of the password:\n"))
sample = "abcdefghijklmnopqrstuvwxyz0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ!@#$%^&*()?"
password = "".join(random.sample(sample,password_length))
print(password)

